# 使用说明 · industry-report-update-skill（v1.0.0 · 开源版 · Apache-2.0）

本文说明如何触发本 Skill，以及每一阶段怎么做、输入什么、产出什么。
流程全貌与纪律分级见 `SKILL.md`；脚本参数见 `scripts/README.md`；详细 SOP 见 `docs/USAGE.md`。

## 一、如何触发

```
帮我更新机器人行业月报（触发起始）
/industry-report-update-skill（命令方式）
```

触发词：`行业月报更新` / `月报更新` / `生成最新月报` / `更新月报` / `generate monthly report`

触发后，Agent 会要求你提供「上一期月报文档（docx）」。

## 二、输入与输出

| 项 | 说明 |
| --- | --- |
| **必填输入** | 上一期行业月报 docx |
| **可选输入** | 数据截至日期、专题研究要求、来源标注粒度、赛道口径（若旧月报无定义） |
| **主要输出** | 最新月报 docx（结构保真 ≥95%） |
| **归档输出** | `runs/<run-id>/`：新月报、变更摘要、映射表、来源记录、溯源.jsonl（带锚点）、通道实测.jsonl、manifest.json |
| **门禁输出** | 11 道门禁报告（run.py 一键生成于 logs/）+ 门禁状态.json（断点续跑） |
| **工作区归档** | `<工作区>/<产品名>_产出/`（Step 9.5 强制收尾） |

## 三、操作详解（10 步）

### Step 0 · 数据流审计（Agent 自动）
`extract_numbers.py` 提取旧月报结论句，补全「数据源/口径/时点」，区分时点型与结构型。

### Step 0.5 · 通道健康度自检（硬性前置）
`channel_health_check.py` 探测 HTTP 通道；**MCP / agent 通道由 Agent 真调一次**并按行回写 `通道实测.jsonl`（status: ok / degraded / fail），脚本校验新鲜度。连续 2 期 ❌ 自动提示续费/改接口。

### Step 1 · 理解构成 + 口径快照 + 运行契约
`manifest.py init` 建立本期契约；`snapshot.py` 落盘口径快照并与上期做叶子级深 diff，**差异 ≥3 项会暂停请你确认**；同时提取目录树 + 格式基准。

### Step 2 · 映射信息获取途径
先盘点本机工具并 smoke test（未实测标 🟡），再逐单元写「具体工具名 + 覆盖度 + 降级链」，生成《信息获取途径映射表.docx》。

### Step 3 · 能力提升分析
汇总 ❌/🟡，说明还需补哪些能力。

### Step 4 · 征询意见（一次性，必填 ≤5 条）
确认清单含「必填项（带默认值 + 反推依据）+ 建议项」。你可以逐项批注，或回复「**一键全采**」。
确认结果落盘 `runs/<run-id>/input/用户确认.md`。

### Step 5 · 副本修改生成（保格式 + 断点）
按采集清单类型处理（结构型复制 / 半结构型核变化 / 时点型重采）；每完成一项 `checkpoint.record()`，中断后自动续跑；改写用 `docx_utils.py` 保留格式；图表走双轨制（保留原图 + 脚注 + 数据表必更新）。
专题对比由 `专题研究.skill` 独立完成，大月报只引用结论。

### Step 6 · 循环修正（最多 3 轮）
`run.py` 一键跑 11 道门禁；中断加 `--resume` 跳过已通过门禁。磨不平的差距（如 TOC 需 F9）如实报告。

### Step 7 · 来源归档 + 数值回读
源文件「章节 + 具体来源」命名、金额标财报期；`溯源.jsonl` 数值类带锚点；
`verify_value.py` 回读比对（月报数字 ≠ 源文件 = 硬伤）；`traceability_check.py` 反查覆盖率；
《变更摘要.md》含「异常波动说明」（环比 ±50% 以上与新增/移除标的逐条点名，门禁回查）。

### Step 8 · 独立盲审
审核子 Agent **只看**新月报 + 源文件（不给作者推理），按 阻断 / 建议 / 提示 分级输出；
阻断级问题修正后换新审核 Agent 复审；通道失败 ≥2 次降级主 Agent 自查并标注「独立性受损」。

### Step 9 · 归档 + 度量
六件套归档到 `runs/<run-id>/`，manifest 更新前置状态，`metrics.py record` 记录本期质量指标，
`metrics.py trend` 随时看跨月趋势。

### Step 9.5 · 工作区镜像（P0-11 强制收尾）
`archive_to_workspace.py` 把全产出镜像到**当前对话工作区** `<工作区>/<产品名>_产出/`。
**open-source 可移植要求**：Agent 必须显式传入工作区（`--anchor` 或 `--ws`），不得依赖本 Skill 所在运行环境。

## 四、常见问题

**Q：旧月报被 WPS/Word 占用，无法覆盖？**
A：Agent 输出到临时文件 `.tmp.docx`，提示你关闭文档后覆盖。

**Q：某板块数据源没接口？**
A：如实标注「本期无法获取」，绝不编造；并列入变更摘要缺口清单。

**Q：目录页码/标题没更新？**
A：目录是 Word 自动域，脚本无法刷新，需你在 Word/WPS 中按 F9。

**Q：法定代表人等工商字段留空了？**
A：这是缺陷。`diff_empty.py` 会按业务键拦截「旧有值 → 新空值」，Agent 用 qcc MCP 逐家补填。

**Q：月报数字和源文件对不上会怎样？**
A：`verify_value.py` 会逐条回读源文件比对，不一致即硬伤，必须修正后才能交付；
差 10/100/10000 倍量级会提示「单位量级疑似不一致」。

**Q：图表怎么处理？**
A：双轨制 —— 原生图表保留原图 + 图下脚注「数据截至 YYYY-MM，最新见表 X」，对应数据表列为必更新项；
图片类图表重生成后替换。

**Q：能用于非机器人行业吗？**
A：能。主流程是通用引擎：`python scripts/pack.py wizard 旧月报.docx --name <新行业>`
反推生成行业包骨架（标的池为候选召回，需逐家人工确认），补全后 `pack.py activate <新行业>` 即可。

## 五、开源相关

- **协议**：Apache-2.0（详见 `LICENSE`)
- **变更记录**：详见 `CHANGELOG.md`
- **决策契约**：详见 `discovery.json`
- **评估**：详见 `evals/industry-report-update.eval.md`

## 六、获取帮助

- 项目主页：https://github.com/ZGYXTTK/industry-report-update-skill
- 问题反馈：GitHub Issues
- 文档：见 `docs/USAGE.md` / `docs/EXAMPLES.md` / `references/discipline.md` / `references/gotchas.md`