# 修改示例 · 行业月报更新 Skill（脱敏通用版）

> 本示例以**虚构的"人形机器人"行业月报**从 M 月更新到 M+1 月为例，演示本 Skill 的完整落地。
> 涉及公司/法人名均已匿名化（A 公司/B 公司/CEO 张三），所有数字均为示例。
> 实战版的"真实教训"沉淀见 `references/gotchas.md` 与 SKILL.md 的"Gotchas"段。

---

## 示例背景

- 基准月报：`人形机器人行业月报2026.M.docx`（虚构）
- 新月报：`人形机器人行业月报2026.M+1.docx`（虚构）
- 数据截至：M+1 月末日
- 专题研究：A 公司 vs B 公司（虚构双公司对比）

## Step 1 · 理解构成（含数据流审计）

**结构示例**：150 段落 + 13 表格，三大章（行业动态 / 资本市场动态 / 专题研究）。

**数据流审计——结论性语句清单（节选，每条都反推数据源/口径/时点）**：

| 结论句 | 数据源 | 口径 | 时点型? |
| --- | --- | --- | --- |
| "截至 M 月末，人形机器人赛道共有 N 家公司在审" | 沪深北交易所审核项目官网 | "人形机器人"×"在审"状态定义×M 月末 | ✅ 必须重采 |
| "共有 M 家辅导备案" | 证监会 eid.csrc.gov.cn | 人形机器人×辅导备案状态 | ✅ 必须重采 |
| "港股 H 家企业在审" | 披露易 | 人形机器人×在审 | ✅ 必须重采 |
| "R 家再融资审核" | 上交所/深交所再融资 | 人形机器人×审核状态 | ✅ 必须重采 |
| 封面/章节/表格列结构 | — | 结构型 | ❌ 可复制 |

**教训**：早期版本把"N 家在审"直接沿用上月、只改 2 家状态，导致漏统、错标板块。**正确做法是重新拉全量候选池→按口径过滤→再计数**，最终重采得 N 家（科创板/创业板/北交所分别 X/Y/Z）。

## Step 2 · 映射（节选）+ 生成映射表 docx

| 最小单元 | 途径 | 覆盖 |
| --- | --- | --- |
| A 股审核状态 | 上交所 commonExcelKcb.do / 深交所 projectrends / 北交所 infoResult.do（POST） | ✅ |
| 辅导备案 | 证监会 eid.csrc.gov.cn/fd.html（**http**，翻页 index_N.html） | ✅ |
| 法定代表人 | 企查查 qcc get_company_registration_info | ✅ |
| 非上市融资 | IT 桔子 search_events / iFinD 新闻 | ✅ |
| 港股在审 | 披露易（**无全量名单接口**，用基线 ± 变化推算并标注不确定性） | 🟡 |

## Step 5 · 副本修改（关键：保留格式地改写）

```python
# ❌ 禁止（丢字体/字号/对齐/缩进）：
cell.text = value
p.text = value
document.add_paragraph(value)
document.add_table(rows, cols)

# ✅ 正确：保留格式只改文字
from docx_utils import set_cell_keep, set_para_keep, fill_table, strip_vmerge, add_row_copy_fmt
set_cell_keep(cell, value)            # 单元格：保留 run 结构
set_para_keep(p, text)                # 段落：首 run 写、其余清空
fill_table(table, rows, start_row=1)  # 表格回填：自动删多余/克隆补足
strip_vmerge(table)                   # 重填前清 vMerge 残留
add_row_copy_fmt(table, values)       # 新增行：克隆格式（含 gridSpan）
```

**教训**：早期用 `cell.text =` 重写表格，导致所有数据行字号丢失（变 inherit）、对齐/垂直居中丢失；事件详情"（N）公司："整段加粗信息也丢了。修复方法：用 v2 函数族逐单元格/逐段落回填。

## Step 6 · 循环修正（双清单对比）

**清单 A · 格式对比发现**：
- ❌ 数据行字号丢失（`cell.text` 覆写）→ 修复：保留格式改写
- ❌ 事件详情标题未加粗、无首行缩进、无段前间距 → 修复：拆"标题加粗 run + 正文 run"，缩进 2 字符、before= 156、line= 360
- ❌ 数据来源段落字号错误 → 修复：9pt
- ❌ 政策/事件日期排序杂乱 → 修复：按事件日期升序重排，表格与正文同步重排

**清单 B · 内容完整性对比发现（防漏填）**：
- ❌ **法定代表人列漏填多处**（早期"查证不稳就填 -"，但上月同类行都有值）→ 修复：企查查逐家核实补填
- ❌ 港股发行漏某家（某日上市）→ 修复：补为 4 家发行、申报 23 家
- ❌ A 股在审沿用 N 家未重采 → 修复：重采为 M 家

**教训**：前三轮只做格式对比、没做空值对比，所以漏填一直没被发现。**今后 Step 6 必须并行做两套对比，且交付前跑"空值对比报告"**。

## Step 7 · 来源归档

- `来源记录.xlsx`：序号/类别/机构/日期/URL
- `下载资料/` 源文件表格，命名「**章节 + 具体来源**」（不写笼统"来源"）：
  - `上交所官网-A股IPO审核-科创板在审.csv`
  - `北交所官网-A股IPO审核-在审.csv`
  - `证监会官网-A股辅导备案-机器人企业.csv`
  - `沪深北交易所官网+iFinD-A股IPO在审企业汇总.csv`
  - `港交所披露易+iFinD-港股IPO发行-M月.csv`
- **金额标财报期/来源**（如"营业收入(亿元, 2025 年报)"、"发行价(港元/股, 发行时点)"、"交易金额(亿元, 公告披露口径)"）
- **新月报正文金额不标财报期**（除非旧月报本身标了），财报期标注只出现在源文件表格

**教训**：早期把"来源-"当固定前缀、且没标财报期；后改为具体来源名（上交所官网/证监会官网/同花顺 iFinD…），并在源文件补财报期标注。

## 关键技术教训（避坑，已固化为 SKILL.md / references/gotchas.md 纪律）

1. **禁止 `cell.text` / `p.text` 覆写** → 保留格式地改写，新增行复制格式
2. **删除章节跳过 `<w:sectPr>`** → 否则页边距丢失
3. **锚点统一用 element**（`p._element` / `tbl._tbl`）+ `addnext`
4. **docx 被占用** → 输出 `.tmp.docx`，等用户关闭后覆盖
5. **交易所接口坑**：北交所 `excelExport.do` 返回 0 字节，改用 POST `infoResult.do`；证监会辅导备案用 http 非 https；深交所 `projectrends` 分页可能失效
6. **法定代表人等工商字段** → 企查查 qcc 逐家核实，不靠记忆留空
7. **空值合理性** → 凡 "-" 必须核实旧月报同类行，非空则必补
8. **字段口径一致** → 法人统一工商法人名，不混用创始人
9. **日期排序** → 政策/事件按事件日期升序，表格与正文详情同步重排 + 重编号
11. **完整枚举不按记忆** → "N 家/N 笔"拉全量候选池再计数，不沿用旧值

---

## 与"实战版"的差异

本示例刻意**完全脱敏**——所有公司/法人/数字均替换为占位符。
实战版（含真实数据沉淀）见 `references/gotchas.md` 的 20 条避坑 + `SKILL.md` 的 Gotchas 段。
两者内容互不冲突，本示例是给从未使用过本 Skill 的用户的"教科书"，实战版是给已经踩过坑的用户的"复查清单"。