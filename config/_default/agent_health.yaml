# =====================================================================
# agent_health.yaml · _default 兜底版（脱敏骨架）
# ---------------------------------------------------------------------
# 用途：subagent 与 workflow 通道连续失败计数器（每月 1）
# 维护：每次月报执行后更新
# 触发：连续 2 次 ❌ 提示「mainagent 兜底」；连续 3 次 ❌ 暂停执行
# 公开版：留空骨架，由用户激活后按实际运行结果填充。
# =====================================================================

版本: "1.0"

历史: []
  # 示例结构（用户激活后按实际填）：
  # - 月份: "YYYY-MM"
  #   subagent:
  #     启动次数: <N>
  #     失败次数: <N>
  #     状态: "✅|🟡|❌|未使用"
  #     失败明细: "<简要描述>"
  #     处置: "<下月建议>"
  #   workflow:
  #     启动次数: <N>
  #     失败次数: <N>
  #     状态: "✅|🟡|❌|未使用"
  #     处置: "<下月建议>"

# 当前状态（供下月开局参考）
当前状态:
  subagent: "未使用"
  workflow: "未使用"
  mainagent_fallback: "✅（mainagent 通道正常）"

# 触发提示
提示:
  subagent连续失败: 0
  workflow连续失败: 0
  是否暂停: false